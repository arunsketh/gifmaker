# AVI to GIF Converter (Streamlit)

Password-protected Streamlit app to convert `.avi` videos to animated GIFs with control over FPS, width, trim range, looping, and optimization.

## Setup

1. Clone this repo.
2. Create virtualenv and install dependencies:

